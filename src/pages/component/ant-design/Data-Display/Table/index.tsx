import React from 'react';
import { Card } from 'antd';
import styles from './index.less';

export default () => {
  return (
    <Card>
      <h1 className={styles.title}>Page Table</h1>
    </Card>
  );
};
